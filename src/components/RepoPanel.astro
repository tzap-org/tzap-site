---
interface Props {
  title: string;
  body: string;
  href: string;
  action: string;
}

const { title, body, href, action } = Astro.props;
---

<section class="repo-panel">
  <div>
    <h2>{title}</h2>
    <p>{body}</p>
  </div>
  <a class="button secondary" href={href}>{action}</a>
</section>
